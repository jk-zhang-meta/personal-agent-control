{
  "schemaVersion": 1,
  "providers": [
    {
      "name": "codegraph",
      "kind": "mcp-server",
      "versionPolicy": "pinned",
      "version": "1.6.0",
      "command": "mise",
      "args": ["--cd", "{{PAC_ROOT}}", "exec", "--", "codegraph", "serve", "--mcp", "--no-watch"],
      "hosts": {
        "codex": {
          "config": ".codex/config.toml",
          "format": "codex-toml"
        },
        "claude": {
          "config": ".claude.json",
          "format": "claude-json"
        }
      }
    }
  ]
}
